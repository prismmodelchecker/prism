#!/bin/csh

foreach K ( 2 4 6 )
	echo "Generating for K=$K"
	prismpp .csmaK.pctl.pp $K >! csma$K.pctl
	foreach N ( 2 3 4 )
		echo " and N=$N"
		prismpp .csmaK_N.nm.pp $N $K >! csma$K"_"$N.nm
	end
end
